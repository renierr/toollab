import 'package:flutter/material.dart';

class CompassColors {
  CompassColors._();

  static const Color dialBackground = Color(0xFF0F172A); // Dark slate
  static const Color dialBorder = Color(0xFF334155); // Muted border slate
  static const Color tickMarkMajor = Color(0xFF94A3B8); // Bright ticks
  static const Color tickMarkMinor = Color(0xFF475569); // Muted ticks
  static const Color needleNorth = Color(0xFFF43F5E); // Neon rose/red
  static const Color needleSouth = Color(0xFF64748B); // Muted blue-grey
  static const Color cardinalNorth = Color(0xFFF43F5E); // Highlight N in red
  static const Color cardinalMuted = Color(0xFFE2E8F0); // Bright off-white
  static const Color textMuted = Color(0xFF94A3B8);
  static const Color accentNeon = Color(
    0xFF22C55E,
  ); // Neon green for calibration/level
  static const Color shadowColor = Color(0x3F000000);
}
